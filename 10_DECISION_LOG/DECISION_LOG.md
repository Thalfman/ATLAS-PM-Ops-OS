# DECISION_LOG.md

## Purpose

This log captures important ATLAS PM/Ops OS decisions so the project remains consistent across sessions. Record decisions that affect structure, safety posture, artifact direction, migration strategy, or operating rules.

## Decision log

| ID | Date | Decision | Rationale | Impact | Status |
|---|---|---|---|---|---|
| D-0001 | 2026-05-22 | Build ATLAS as a Markdown-first operating system, not an app. | The immediate need is PM/Ops structure, templates, prompts, workflows, and governance, not software infrastructure. | No package dependencies, frontend/backend code, APIs, databases, or deployment configs in Phase 0. | Active |
| D-0002 | 2026-05-22 | Treat Gemini Enterprise as the likely first approved AI tool while keeping workflows platform-agnostic. | The workplace may initially approve Gemini only, but workflows should migrate to other approved tools later. | Prompts and workflows avoid tool-specific assumptions where possible. | Active |
| D-0003 | 2026-05-22 | Use only synthetic, public, generic, fictional, or user-created non-proprietary examples during pre-start work. | The role is federal/defense-adjacent and may involve sensitive data after onboarding. | All starter artifacts avoid real employer data. | Active |
| D-0004 | 2026-05-22 | Make AI integration a primary value workstream, not a side topic. | Tom's AI expertise was identified during hiring as part of expected value. | Roadmap, backlog, workflows, and prompts emphasize practical AI-enabled PM/Ops improvements. | Active |
| D-0005 | 2026-05-22 | End significant work by updating `09_HANDOFFS/SESSION_HANDOFF.md`. | Continuity is critical for a durable operating system. | Handoff file becomes the current-state anchor for future sessions. | Active |
| D-0006 | 2026-05-22 | Place the Phase 0 through Phase 12 list inside `MASTER_CONTEXT.md`, not only in `MASTER_PLAN.md` or the prompt index. | The durable source of truth should carry the full phase plan so chat-only sessions can see it without browsing the repo. | `MASTER_CONTEXT.md` is the single place to read the phase plan; updates must be mirrored from `MASTER_PLAN.md` and the prompt index. | Active |
| D-0007 | 2026-05-22 | Add explicit Session-start and Session-end protocols to `MASTER_CONTEXT.md`. | Continuity must survive both local-agent and chat-only sessions; the protocols make the loop reproducible. | Every future ATLAS session begins and ends with the same explicit steps. | Active |
| D-0008 | 2026-05-22 | Define a precedence rule: `MASTER_CONTEXT.md` wins on durable identity and safety; `SESSION_HANDOFF.md` wins on current status and the next best prompt. | Eliminates ambiguity when the two files appear to disagree. | Future sessions resolve conflicts deterministically without restarting. | Active |
| D-0009 | 2026-05-22 | Expand `SESSION_HANDOFF_TEMPLATE.md` with source-of-truth review checklist, definition-of-done check (including a decision-log entry), AI tooling notes, and a parameterized next-best-prompt block. | Tightens the continuity loop and prevents drift between sessions. | Every handoff carries the same structured fields and a copy-paste-ready next prompt. | Active |
| D-0010 | 2026-05-22 | Gate the session-end commit step by mode: local-agent commits on a feature branch; chat-only hands Tom a copy-paste commit instruction as a human follow-up. | The previous unconditional commit step was unsatisfiable in chat-only mode. | The session-end protocol is internally consistent for both supported modes. | Active |
| D-0011 | 2026-05-22 | Require chat-only sessions to ask Tom to paste the relevant phase prompt and `DECISION_LOG.md` contents, in addition to `MASTER_CONTEXT.md` and `SESSION_HANDOFF.md`. | Phase work and decision logging cannot be run from memory in chat-only mode. | Chat-only sessions stay aligned with phase-specific instructions and avoid decision-ID collisions. | Active |
| D-0012 | 2026-05-22 | Leave the three `skills/*/SKILL.md` files unchanged in Phase 1. | They already align with the strengthened protocols; edits would be cosmetic and outside Phase 1 scope. | Skill files revisited only when Phase 2+ surfaces a concrete gap. | Active |
| D-0013 | 2026-05-22 | Perform ATLAS Phase 1 work on feature branch `feat/phase-01-master-context-continuity` and open PR #1 against `main`; never edit `main` directly. | Branch-protection guardrail enforced by the repo pre-tool hook; aligns with the global rule "never edit main, master, release, or prod directly." | All ATLAS build phases default to a per-phase feature branch and PR review; no direct commits to `main`. | Active |
| D-0014 | 2026-05-22 | Adopt a standard field set for every backlog row (ID, artifact, category, phase, priority, purpose, intended user, data sensitivity posture, target environment assumption, human review point, status, notes/dependencies). | A consistent row shape makes the backlog usable as the single ordered list for all downstream phases and makes safety posture explicit per artifact. | `03_BACKLOG/ARTIFACT_BACKLOG.md` rows are uniform; any new artifact must populate all fields. | Active |
| D-0015 | 2026-05-22 | Use a fixed set of backlog categories (continuity and project management; governance and tool approval; workflow library; prompt library; first-week readiness; synthetic demos; Microsoft Project and schedule integrity; EVM, finance, and project accounting; SOPs and lessons learned; employer migration; final review and maintenance). | Matches the Phase 2 prompt's required category list and gives the backlog a stable structure across phases. | Backlog grouping is stable; new categories require a logged decision. | Active |
| D-0016 | 2026-05-22 | Adopt a standard per-phase field set in the roadmap (purpose, key files or artifact areas, PM/Ops outcomes, AI integration relevance, safety and governance considerations, completion criteria, dependencies and sequencing notes). | Matches the Phase 2 prompt's required roadmap fields and makes each phase reviewable on its own merits. | `02_ROADMAP/ROADMAP.md` carries the same fields for every phase; revisions must keep the field set intact. | Active |
| D-0017 | 2026-05-22 | Treat artifact IDs as append-only; retire by setting status to `Deferred` or writing a "Retired - see A-NNNN" row instead of reusing an ID. | Stable IDs let other files reference artifacts without ambiguity over time. | All cross-references in the backlog, roadmap, and future workflow/prompt files stay valid even when scope changes. | Active |
| D-0018 | 2026-05-22 | Document the source-of-truth alignment rule: `MASTER_CONTEXT.md` for durable identity and safety, `SESSION_HANDOFF.md` for current status and next best prompt, `ROADMAP.md` for per-phase detail, `ARTIFACT_BACKLOG.md` for per-artifact fields. | Phase 2 adds detail to two new files; the rule prevents drift across `MASTER_CONTEXT.md`, `MASTER_PLAN.md`, `ROADMAP.md`, and `ARTIFACT_BACKLOG.md`. | Future sessions resolve conflicts deterministically and update the right file. Captured in `MASTER_PLAN.md` and reinforced in `MASTER_CONTEXT.md`. | Active |
| D-0019 | 2026-05-22 | Phase 4 (workflow library) and Phase 5 (prompt library) must wait on Phase 3 (governance) so they can inherit a common human review point pattern and data envelope. | Without the governance bundle, workflows and prompts would each invent their own safety language and drift. | The roadmap and backlog explicitly sequence Phase 3 before Phases 4 and 5; Phase 3 prompt is named in the Phase 2 handoff. | Active |
| D-0020 | 2026-05-22 | Perform ATLAS Phase 2 work on feature branch `feat/phase-02-roadmap-and-backlog`, branched off `feat/phase-01-master-context-continuity` while PR #1 is open. | Phase 2 depends on Phase 1's hardened context and protocols; branching from the Phase 1 branch keeps the diff focused on Phase 2 deliverables while PR #1 is in review. | Phase 2 PR targets `main` once PR #1 merges, or rebases onto `main` post-merge. No direct commits to `main`. | Active |
| D-0021 | 2026-05-22 | Add a repo-level `CLAUDE.md` at the repo root that codifies (a) source-of-truth file order, (b) the safety boundary one-liner, (c) the "what not to add" list, (d) a standing Codex review loop where Claude addresses unresolved Codex review threads, commits on the PR branch, pushes, and resolves threads without asking permission, (e) commit and branch policy, and (f) when to update `CLAUDE.md`. | Tom explicitly requested standing automation: "address Codex comments and commit and push again always." Capturing the rule in a repo-loaded `CLAUDE.md` makes it active in any future Claude Code session in this directory and survives session resets. Logging here keeps `DECISION_LOG.md` and the operating rules synchronized. | All future ATLAS Claude Code sessions in this repo load the rule automatically. Codex review feedback on any ATLAS PR is addressed in-loop without requiring a separate Tom prompt. The rule never overrides hard limits (no edits to `main`, no force-push on shared branches, no skipped hooks, no employer-sensitive material, no safety-boundary violations). | Active |
| D-0022 | 2026-05-22 | Authorize one explicit exception to the "no apps, packages, APIs, databases, deployment files, or code scaffolding" rule for ATLAS: add `.github/workflows/codex-review-loop.yml`, a single GitHub Actions workflow that runs `anthropics/claude-code-action@v1` to address Codex review feedback on PRs without manual prompting. Triggers: `pull_request_review` (submitted), `pull_request_review_comment` (created), and `issue_comment` (created) filtered to `chatgpt-codex-connector` author or `@claude` mention; plus `workflow_dispatch` for manual runs. Auth via repo secret `CLAUDE_CODE_OAUTH_TOKEN` (Tom's personal Pro/Max subscription token, generated locally with `claude setup-token`); the workflow does not use `ANTHROPIC_API_KEY` and must not be reconfigured to use one. | Tom explicitly requested "set up GitHub actions" to make Codex auto-handling fire-on-event rather than fire-when-Claude-is-running, and explicitly stated he uses only his personal Claude plan and not API keys. The CI exception is narrow (one file, one purpose) and the workflow only addresses review feedback inside the existing ATLAS safety boundary and decision log. Any further CI files require a new logged decision. | Codex review feedback on any ATLAS PR is addressed automatically via CI as soon as a Codex review or comment is posted, in addition to the in-session rule in D-0021. Cost lands against Tom's Pro/Max subscription usage, not a separate Anthropic API bill. Workflow cannot edit `main`, force-push shared branches, skip hooks, introduce employer-sensitive material, or add further CI files. | Active |
| D-0023 | 2026-05-22 | Adopt three named review intensities (Light, Standard, Strict) as the only review levels ATLAS uses, defined in `06_GOVERNANCE/HUMAN_REVIEW_AND_AUDITABILITY_MODEL.md`. | A small, fixed set keeps every Phase 4 workflow and Phase 5 prompt consistent and prevents per-artifact reinvention of review language. Three levels cover the realistic range (synthetic practice through employer-deployable). | All future workflows and prompts cite one of the three intensities; the model file is the single source of truth for what each intensity requires. | Active |
| D-0024 | 2026-05-22 | Adopt the five data categories (Synthetic, Public, Tom-personal, Employer-approved, Prohibited) and four tool environments (ATLAS-local Markdown, Personal AI tool, Employer-approved AI tool, No AI tool) from `06_GOVERNANCE/DATA_SENSITIVITY_DECISION_MODEL.md` as the canonical classification model for ATLAS. | Every workflow and prompt needs a shared vocabulary for "what kind of data is this" and "where can it go." A fixed list prevents drift and makes routing decisions reviewable. | Workflows and prompts reference these categories and environments by name instead of redefining sensitivity language; uncertain inputs default to Prohibited. | Active |
| D-0025 | 2026-05-22 | Adopt the six-step AI tool approval pattern (identify candidate, classify data path, identify approved tool environment, define human review point, pilot safely, document/request approval/record decision) in `06_GOVERNANCE/AI_TOOL_APPROVAL_STRATEGY.md` as the required pattern before any AI use case touches employer data. | A single, conservative pattern is cheaper to follow than reinventing approval logic per workflow, and it makes refusal/escalation defensible because the steps are visible. | New candidate AI use cases must walk all six steps before real-data use; the intake template in that file is the personal preparation recording mechanism. | Active |
| D-0026 | 2026-05-22 | Distinguish personal preparation artifacts (live in ATLAS, synthetic/public/Tom-personal only) from employer-deployable artifacts (live in the approved employer environment, require explicit approval and audit trail) as a core ATLAS concept, defined in `06_GOVERNANCE/AI_GOVERNANCE_NOTES.md`. | The same artifact pattern needs two lifecycles; conflating them is the most likely path to a safety-boundary violation during migration. | Phase 11 (Employer Migration) inherits this distinction; workflows and prompts authored in Phase 4 and Phase 5 are personal preparation artifacts by default, with employer-deployable versions produced separately when approvals exist. | Active |
| D-0027 | 2026-05-22 | Default to conservative retention for AI prompts and outputs while employer policy is unknown, per `06_GOVERNANCE/PROMPT_AND_OUTPUT_RETENTION_NOTE.md`: synthetic/public material retained in repo, Tom-personal material retained only as needed and pruned, employer-approved material retained only inside the approved environment, prohibited material not created (and deleted if created). | A documented default that errs on the restrictive side keeps personal-preparation use safe and makes it easy to swap in employer policy later without re-arguing the principles. | This file is the default until employer policy is known; revisit triggers are listed in the file. | Active |
| D-0028 | 2026-05-22 | Perform ATLAS Phase 3 work on feature branch `feat/phase-03-governance-and-tool-approval-strategy` branched off `main` after PRs #1, #2, and #3 merged. | Phase 1 and Phase 2 are merged to `main`; the next phase branches cleanly off `main` per the repo `CLAUDE.md` branch policy ("feat/phase-NN-<short-slug>"). | Phase 3 PR targets `main` directly. Subsequent phase branches follow the same `feat/phase-NN-<short-slug>` convention. | Active |
| D-0029 | 2026-05-23 | Adopt a 16-section workflow card schema for all Phase 4 cards: 12 required fields from the Phase 4 prompt (workflow name, PM/Ops problem, intended outcome, safe inputs, prohibited inputs, output format, tool assumption, human review point, step-by-step process, quality checks, audit/logging notes, failure modes plus migration notes) plus 4 governance citations consolidated in §4 (data category, tool environment, review intensity, per-domain review pattern from the Phase 3 bundle) plus identity header and cross-references. Captured in `04_WORKFLOWS/WORKFLOW_CARD_TEMPLATE.md`. | A single named schema prevents per-card reinvention, makes the Phase 4 prompt's acceptance criteria one-to-one gradable, and gives Phase 5 prompts a stable per-card anchor to pair against. The four governance citations carry forward D-0023 (review intensities) and D-0024 (data categories and tool environments) without restating them. | All current and future workflow cards conform to the schema. Any schema change updates the template first, back-ports every existing card in the same session, and logs the change. | Active |
| D-0030 | 2026-05-23 | Adopt `W-NN-<short-slug>.md` naming for per-workflow files under `04_WORKFLOWS/`. IDs are append-only; numbering follows the Phase 4 prompt's enumerated list 1-15, and continues from W-16 for any additional workflows. | Stable IDs let prompts, demos, migration plans, and review threads reference workflow cards unambiguously across phases. Mirrors the append-only A-ID discipline in D-0017. | All future workflow references use the W-NN ID. New workflows take the next available number; retired workflows are marked Deferred, never renumbered. The library index in `WORKFLOW_LIBRARY.md` is the canonical map from W-ID to file path. | Active |
| D-0031 | 2026-05-23 | Structure `04_WORKFLOWS/` as a hybrid - library index (`WORKFLOW_LIBRARY.md`) + reusable template (`WORKFLOW_CARD_TEMPLATE.md`) + one file per workflow (`W-NN-<slug>.md`) - instead of a single long library file. | The 16-section schema makes single-file storage merge-hostile (each card runs ~150-200 lines, so 15 cards in one file would exceed 3,000 lines). Per-workflow files give Codex review per-line precision and align with the Phase 2 roadmap's anticipation of "per-workflow Markdown files." The 8 starter cards previously inline in `WORKFLOW_LIBRARY.md` are folded into the matching W-NN files; the library file becomes the index, not a card store. | All current and future workflows get their own file. The library file holds universal rules, governance references, schema reference, and the workflow index table. Phase 5 prompts pair against W-NN files by ID. | Active |
| D-0032 | 2026-05-23 | Add three new Phase 4 backlog rows to `03_BACKLOG/ARTIFACT_BACKLOG.md` under "Workflow library": A-0066 EVM Variance Explanation Support Workflow, A-0067 Executive Brief Generation Workflow, A-0068 Clearance-Limited Onboarding Workflow. The four prompt workflows whose primary backlog assets are anchored to later phases (A-0013 lessons learned, A-0014 SOP draft generation, A-0015 schedule health, A-0016 schedule variance) are not duplicated; their Notes column points at the matching Phase 4 W-NN file. A-0038 Process Gap Note Workflow remains `Not started` for a follow-up session and is not built in Phase 4. | The Phase 4 prompt enumerates 15 minimum workflows but only 8 had Phase 4 backlog rows; promoting the truly missing 3 preserves the single-ordered-list discipline (D-0014, D-0017). Reusing later-phase A-IDs for the workflow cards that pair with deeper Phase 8/10 deliverables avoids two-rows-one-workflow drift. A-0038 stays in the backlog at Phase 4 P1 so it can be picked up in a follow-up session without re-litigating scope. | The backlog grows by three rows; four rows get cross-references added; 8 Phase 4 rows and the 3 new rows flip to `Ready for personal use`. Phase 5 has 15 workflow cards to pair prompts against. A-0038 is the obvious next-up Phase 4 follow-up. | Active |
| D-0033 | 2026-05-23 | Perform ATLAS Phase 4 work on branch `claude/phase-4-planning-4142G` rather than the conventional `feat/phase-04-workflow-library`. CLAUDE.md commit and branch policy otherwise applies (Conventional Commits, no direct edits to `main`, PR against `main`, no force-push on shared branches, no skipped hooks). | The branch was created by the operating environment before the session began; switching branches mid-flight would create churn without benefit. The branch behaves identically to a `feat/phase-NN-<slug>` branch for review and merge purposes. The deviation is narrow and not a precedent for future phases. | Future phase branches return to the `feat/phase-NN-<short-slug>` convention unless similarly constrained. The decision is logged here so a future reader finds the deviation in the canonical place. | Active |
| D-0034 | 2026-05-23 | Refine the workflow card schema (D-0029) in two ways, applied uniformly across all 15 Phase 4 cards in response to Codex review on PR #5: (a) §6 "Prohibited inputs" opens with a scoping preamble stating the prohibition applies in the default personal-preparation environment from §4, and that employer-deployable use against employer-approved data requires explicit approval per §14 and the six-step pattern in `06_GOVERNANCE/AI_TOOL_APPROVAL_STRATEGY.md`; (b) §15 "Cross-references" includes a "Paired Phase 5 prompt" line on every card - either naming the A-ID for the paired prompt, or explicitly stating "none" with a short reason for cards (W-04, W-13, W-14, W-15) whose role is structural, investigative, information-management, or personal-planning rather than AI-drafting. | Codex flagged a real internal contradiction: §6 read as an absolute ban on real employer data, while §4/§8/§14 described approved employer-deployable use of that same data after approval. The scoping preamble removes the contradiction without weakening the safety boundary. The "Paired Phase 5 prompt" line eliminates the implication that all 15 W-NN cards must have paired prompts: the backlog defines exactly 11 prompt-library rows (A-0018, A-0019, A-0039..A-0047), and four workflows do not need a paired prompt. This prevents Phase 5 from being forced to invent out-of-scope prompts to satisfy a 15/15 mapping. | Backlog and decision log reflect a deliberate 11/15 prompt pairing. The schema (`WORKFLOW_CARD_TEMPLATE.md`) and the index (`WORKFLOW_LIBRARY.md`) inherit the refinements implicitly via the per-card edits; the template itself is also updated to include the §6 preamble and the "Paired Phase 5 prompt" line in §15 so future cards conform without re-litigation. | Active |
| D-0035 | 2026-05-23 | Adopt `P-NN-<short-slug>.md` naming for prompt cards under `05_PROMPTS/`. IDs are append-only. Numbering parallels the paired W-NN where one exists (P-01..P-12 pair W-01..W-12); the four positions whose paired W-NN carries "Paired Phase 5 prompt: none" per D-0034 are intentionally reserved-but-empty (no P-04, P-13, P-14, P-15). Two utility prompts take fixed IDs: `P-00-safety-precheck.md` (universal pre-flight) and `P-99-prompt-critique-and-output-qa.md` (post-flight critique). | Stable IDs let workflow cards, demos, migration plans, and review threads reference prompts unambiguously across phases. Mirrors the append-only A-ID rule (D-0017) and W-ID rule (D-0030). The P-ID-to-W-ID parallel keeps the pairing readable; reserved gaps make the 11/15 pairing visible at a glance. Utility prompts at P-00 and P-99 separate them from the paired numbering. | All current and future prompt references use the P-NN ID. New paired prompts take the next available number after the existing set; retired prompts are marked Deferred, never renumbered. The library index in `PROMPT_LIBRARY.md` is the canonical map from P-ID to file path. | Active |
| D-0036 | 2026-05-23 | Adopt a 14-section prompt card schema for all Phase 5 prompt cards, captured in `05_PROMPTS/PROMPT_CARD_TEMPLATE.md`. The 14 sections cover the 7 required fields from the Phase 5 prompt (use case, safe input requirements, prohibited input warning, copy-paste prompt text, expected output, human review checklist, notes for approved-tool migration) plus a governance envelope citing the Phase 3 bundle, a paired-workflow citation, a placeholders list, failure modes and escalation triggers, a safety-precheck citation, an output-critique pass citation, an identity header, and cross-references. Structurally mirrors the Phase 4 workflow card schema (D-0029, D-0034). | A single named schema prevents per-prompt reinvention and makes every Phase 5 deliverable gradable against the same 7-field acceptance criteria. Mirroring the workflow card schema means every paired P-NN and W-NN pair are structurally aligned, which makes Codex review and human review predictable. Consolidating governance, paired-workflow, and utility-prompt citations into named sections avoids restating policy at the prompt layer. | All current and future prompt cards conform to the schema. Any schema change updates the template first, back-ports every existing P-NN file in the same session, and logs the change. | Active |
| D-0037 | 2026-05-23 | Structure `05_PROMPTS/` as a hybrid — library index (`PROMPT_LIBRARY.md`) + reusable template (`PROMPT_CARD_TEMPLATE.md`) + one file per prompt (`P-NN-<slug>.md`) + two utility prompts (`P-00-safety-precheck.md`, `P-99-prompt-critique-and-output-qa.md`) — instead of a single long library file. | The 14-section schema makes single-file storage merge-hostile; per-prompt files give Codex review per-line precision and parallel the Phase 4 hybrid layout (D-0031). The two utility prompts sit outside the 11/15 pairing because they apply universally, not to one workflow; carving them out as their own files prevents per-paired-prompt copies of the same precheck and critique pattern. | All current and future prompts get their own file. The library index holds universal rules, governance references, schema reference, and the prompt index table (11 paired + 2 utility + 4 reserved-but-empty positions). Phase 6+ phase artifacts reference prompts by P-ID. | Active |
| D-0038 | 2026-05-23 | The universal safety precheck is authored once as `05_PROMPTS/P-00-safety-precheck.md` and cited by every paired P-NN card's §12 "Safety precheck" section, rather than restated in each card. Likewise, the output-critique / QA pass is authored once as `05_PROMPTS/P-99-prompt-critique-and-output-qa.md` and cited by every paired P-NN card's §13 "Output critique pass (optional)" section. P-00 is required before every paired prompt; P-99 is required for Strict review intensity and recommended for Standard. Neither utility prompt is a new backlog row; both are tracked in the library index. | One canonical precheck and one canonical critique prevent drift across 11 paired prompts and keep the Phase 3 pre-flight and post-flight checklists (`HUMAN_REVIEW_AND_AUDITABILITY_MODEL.md`) authoritative without restatement at the prompt layer. Tracking the utility prompts in the library index rather than the backlog keeps the backlog as the per-deliverable list and matches the existing precedent for template files (e.g., `WORKFLOW_CARD_TEMPLATE.md` is similarly not a backlog row). | Future edits to P-00 (e.g., a new classification rule from `DATA_SENSITIVITY_DECISION_MODEL.md`) apply universally on the next session, with no per-prompt back-porting. The same is true of P-99. New utility prompts are added to the library index under their fixed IDs, not the backlog. | Active |
| D-0039 | 2026-05-23 | Perform ATLAS Phase 5 work on branch `claude/atlas-pm-ops-phase-UXSqk` rather than the conventional `feat/phase-05-prompt-library`. CLAUDE.md commit and branch policy otherwise applies (Conventional Commits, no direct edits to `main`, PR against `main`, no force-push on shared branches, no skipped hooks). | The branch was created by the operating environment before the session began (paralleling the Phase 4 constraint in D-0033); switching branches mid-flight would create churn without benefit. The branch behaves identically to a `feat/phase-NN-<slug>` branch for review and merge purposes. The deviation is narrow and not a precedent for future phases. | Future phase branches return to the `feat/phase-NN-<short-slug>` convention unless similarly constrained. The decision is logged here so a future reader finds the deviation in the canonical place. | Active |
| D-0040 | 2026-05-23 | Perform ATLAS Phase 6 work on branch `claude/atlas-pm-ops-phase-OhdyH` rather than the conventional `feat/phase-06-first-week-readiness-kit`. CLAUDE.md commit and branch policy otherwise applies (Conventional Commits, no direct edits to `main`, PR against `main`, no force-push on shared branches, no skipped hooks). | The branch was created by the operating environment before the session began (paralleling Phase 4 in D-0033 and Phase 5 in D-0039); switching branches mid-flight would create churn without benefit. The branch behaves identically to a `feat/phase-NN-<slug>` branch for review and merge purposes. The deviation is narrow and not a precedent for future phases. | Future phase branches return to the `feat/phase-NN-<short-slug>` convention unless similarly constrained. The decision is logged here so a future reader finds the deviation in the canonical place. | Active |
| D-0041 | 2026-05-23 | Structure Phase 6 deliverables as a hybrid under `07_TEMPLATES/` — kit master index (`FIRST_WEEK_READINESS_KIT.md`) + six backlog-row files (A-0005, A-0006, A-0022, A-0048, A-0049, A-0050) + one utility template (`AI_INTEGRATION_DISCUSSION_GUIDE.md`) — rather than a single long kit file. | Per-artifact files parallel the Phase 4 workflow hybrid (D-0031) and the Phase 5 prompt hybrid (D-0037), give Codex review per-line precision, and let each artifact name a specific A-ID and visible-artifact contract. A single long kit file would be merge-hostile and would blur per-artifact ownership. The kit master index plays the same indexing role that `WORKFLOW_LIBRARY.md` and `PROMPT_LIBRARY.md` play in their respective phases. | All Phase 6 artifacts get their own file under `07_TEMPLATES/`. Future kit additions (post-clearance variations, role-specific kits) take new A-IDs and follow the same per-file pattern. Schema changes to the kit envelope or "Used by" block update the index first, then back-port every sub-file in the same session. | Active |
| D-0042 | 2026-05-23 | Each Phase 6 kit artifact cites the Phase 3 governance bundle by filename — data category (`DATA_SENSITIVITY_DECISION_MODEL.md`), tool environment (same file), review intensity (`HUMAN_REVIEW_AND_AUDITABILITY_MODEL.md`), per-domain review pattern (same file) — and references W-15 (`04_WORKFLOWS/W-15-clearance-limited-onboarding.md`) as the operating workflow. The envelope is restated in `FIRST_WEEK_READINESS_KIT.md` once and mirrored in each sub-file's "Governance envelope" section; no policy is restated at the artifact layer. | Mirrors the Phase 4 §4 (D-0029) and Phase 5 §3 (D-0036) governance-envelope discipline. Citing rather than restating prevents drift across the seven kit files and keeps the governance bundle authoritative. Naming W-15 as the operating workflow consolidates the kit into a single behavioral pattern (weekly commitments, daily practice, end-of-week reflection) rather than seven independent artifact stories. | Future kit edits change the cited file when the governance bundle changes, not the artifact files. New kit files conform to the same envelope shape. Codex review of the kit can verify the envelope citation in seconds rather than re-litigating policy per file. | Active |
| D-0043 | 2026-05-23 | The AI Integration Discussion Guide is authored once as `07_TEMPLATES/AI_INTEGRATION_DISCUSSION_GUIDE.md` and tracked in the kit master index, not the backlog. It is a utility template, parallel to the P-00 / P-99 utility-prompt pattern (D-0038) in the Phase 5 prompt library and the workflow / prompt card templates in the Phase 4 / 5 hybrids (D-0031, D-0037). It cites `06_GOVERNANCE/AI_CONVERSATION_GUIDE.md` as the parent governance reference and does not restate that file's content. | The Phase 6 prompt explicitly names "a conservative AI integration discussion guide" as a deliverable but the backlog does not include a row for it. Tracking it in the kit index keeps the backlog as the per-deliverable list and matches the existing precedent for utility files (templates and shared utility prompts). Citing the governance conversation guide as the parent reference avoids two documents drifting on the same topic. | Future utility kit artifacts (e.g., a post-clearance variant of the discussion guide) are added to the kit master index under fixed slugs, not the backlog. Edits to `06_GOVERNANCE/AI_CONVERSATION_GUIDE.md` apply through citation; the kit guide only restates kit-layer adaptations. | Active |
| D-0044 | 2026-05-23 | The "What I Can Offer This Week" one-pager (`07_TEMPLATES/WHAT_I_CAN_OFFER_THIS_WEEK.md`, A-0050) must not commit to scope outside the manager-agreed clearance-limited posture. Specifically: every commitment names a visible artifact and a target date, no commitment depends on cleared access Tom does not have, no commitment requires real program/customer/contract/schedule/finance content, and no wording implies authority Tom has not been granted. Red flags mirror W-15 §11 and escalation triggers mirror W-15 §13. | The one-pager is the most likely place a synthetic-only personal-preparation artifact could drift into commitments that overclaim or assume access. Naming the scoping rule once at the decision-log layer keeps the rule authoritative across reviews and future revisions of the one-pager. Mirroring W-15 §11 / §13 prevents the kit from inventing its own red flags and keeps the operating workflow as the single source of truth on what "in scope" means during clearance-limited mode. | Future edits to the one-pager template are checked against this rule before any merge. If a Codex review or manager-conversation outcome would loosen the rule, that change requires a new logged decision before the one-pager is updated. The rule does not apply to other kit artifacts (which have lighter review intensities and different audiences). | Active |
| D-0045 | 2026-05-23 | Perform ATLAS Phase 7 work on branch `feat/phase-07-synthetic-demo-pack` per the canonical `feat/phase-NN-<short-slug>` convention in `CLAUDE.md`. Phases 4, 5, 6 used environment-constrained branches (D-0033, D-0039, D-0040); Phase 7 was not similarly constrained because no env-provided branch was pre-allocated for it. | The branch convention exists for a reason — it labels phase work clearly in the PR list and lets a future reader find the diff that built each phase. Returning to the convention as soon as the environment allows it keeps the deviation pattern (D-0033, D-0039, D-0040) narrow rather than precedential. | Future phase branches default to the `feat/phase-NN-<short-slug>` convention; env-constrained branches are logged as decisions when they occur. | Active |
| D-0046 | 2026-05-23 | Structure Phase 7 deliverables as a hybrid under `08_SYNTHETIC_DEMOS/` — a pack index (`SYNTHETIC_DEMO_PACK.md`), a shared synthetic project scenario (`SYNTHETIC_PROJECT_SCENARIO.md`), and five per-demo backlog-row files (A-0007, A-0023, A-0024, A-0051, A-0052) — rather than a single long demo file. | Per-demo files parallel the Phase 4 workflow hybrid (D-0031), the Phase 5 prompt hybrid (D-0037), and the Phase 6 kit hybrid (D-0041). Each demo names a specific paired W-NN / P-NN and a per-domain review pattern; a single long demo file would blur those pairings. Codex review per-line precision is the secondary benefit. | All Phase 7 demos get their own file. Future demo additions (Phase 8 schedule fidelity, Phase 9 EVM fidelity, Phase 10 lessons-learned fidelity) follow the same per-demo file pattern and pair against existing or future W-NN / P-NN cards. | Active |
| D-0047 | 2026-05-23 | Every Phase 7 demo names its paired Phase 4 workflow card (W-NN) and Phase 5 prompt card (P-NN) by ID in the artifact identity header, opens with a bold synthetic label, walks input → AI step → human review → output as a pattern, and cites the Phase 3 governance envelope by filename. The shared scenario file (`SYNTHETIC_PROJECT_SCENARIO.md`) publishes the envelope once; per-demo files repeat the four key lines for readability and do not restate policy. | The Phase 7 prompt requires demos to be obviously synthetic, to demonstrate AI-assisted workflows without being mistaken for real artifacts, and to be portable for approved-tool migration. Naming W-NN / P-NN pairs makes the migration path explicit (each demo points at the workflow and prompt cards that already carry §14 migration notes); the input → AI step → human review → output structure makes the demo a pattern, not a product. Citing rather than restating the envelope mirrors D-0029 / D-0036 / D-0042. | All Phase 7 demos conform to this structure. Future demos must include the identity header, the synthetic label, the four-step walkthrough, and the governance-envelope citation. Codex review can verify these in seconds rather than re-litigating per file. | Active |
| D-0048 | 2026-05-23 | The shared synthetic project scenario is authored once as `08_SYNTHETIC_DEMOS/SYNTHETIC_PROJECT_SCENARIO.md` and tracked in the pack index, not the backlog. It is a utility file, parallel to the P-00 / P-99 utility-prompt pattern (D-0038) and the AI Integration Discussion Guide (D-0043). Every demo references the scenario for cast (Project Northstar Demo, PM/Ops, Team Lead, Workstream A / B / C Leads, Reporting Coordinator, Placeholder Owner 1 / 2 / 3), milestones (M-01..M-06), and workstreams (Reporting, Action tracking, Risk and decision capture). | One coherent backdrop makes demos walk-through-able with a manager because the same cast and milestones appear across reports; the alternative (independent fictional contexts per demo) would create incoherence and waste the audience's working memory. Tracking the scenario in the pack index rather than the backlog parallels existing precedent for utility files. | Future demos draw from the same scenario; if a future demo needs a second backdrop (e.g., for EVM scope), it is added as a sibling utility file with its own decision. Edits to the scenario apply to all demos via cross-reference; back-port relevant per-demo updates in the same session. | Active |

## Decision entry template

| ID | Date | Decision | Rationale | Impact | Status |
|---|---|---|---|---|---|
| D-XXXX | YYYY-MM-DD | [Decision] | [Why] | [What changes] | Proposed/Active/Retired |
